# Codex Auth

## 方針

この構成では API キーではなく、ローカルにログイン済みの Codex CLI を前提にします。

## 運用メモ

- `~/.codex/` はローカル専用。Git 管理しない
- Bot 実行ユーザーで `codex` コマンドが使える状態にしておく
- 認証が必要な更新は Bot ではなく運用者が手動で行う

## 想定リスク

- セッション切れ時はジョブが失敗する
- 実行ユーザーを変えると認証状態を引き継げない
- 将来 SSH 実行に広げる場合、各ターゲット側でも Codex の認証運用が必要

